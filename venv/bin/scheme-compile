#!/bin/sh
IF="$(realpath "${1}")"
if [ "r7rs" = "r6rs" ]; then OF="${IF%.sps}"; else OF="${IF%.scm}"; fi
printf "
#|
exec /home/retropikzel/Projects/foreign-c/venv/bin/scheme-script \"\${0}\" \"\$@\"
|#
" > "${OF}"
cat "${IF}" >> "${OF}"
chmod +x "${OF}"
